import React from 'react';
import AccountConnectedMenu from '../components/AccountConnectedMenu';

const Account = () => {
    return (
        <div className='accountMenuContainer'>
            <AccountConnectedMenu />
        </div>
    );
};

export default Account;